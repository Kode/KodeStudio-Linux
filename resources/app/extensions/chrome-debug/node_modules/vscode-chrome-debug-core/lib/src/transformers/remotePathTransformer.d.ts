import { BasePathTransformer } from './basePathTransformer';
import { ISetBreakpointsArgs, IAttachRequestArgs, IStackTraceResponseBody } from '../debugAdapterInterfaces';
/**
 * Converts a local path from Code to a path on the target.
 */
export declare class RemotePathTransformer extends BasePathTransformer {
    private _localRoot;
    private _remoteRoot;
    attach(args: IAttachRequestArgs): Promise<void>;
    setBreakpoints(args: ISetBreakpointsArgs): void;
    scriptParsed(scriptPath: string): string;
    stackTraceResponse(response: IStackTraceResponseBody): void;
    private shouldMapPaths(remotePath);
    getClientPathFromTargetPath(remotePath: string): string;
    getTargetPathFromClientPath(localPath: string): string;
}
